const mongoose=require('mongoose');const s=new mongoose.Schema({type:{type:String,enum:['announcement','test','file','book'],required:true},title:String,body:String,subject:String,image:String,url:String,createdBy:{type:mongoose.Schema.Types.ObjectId,ref:'User'},status:{type:String,enum:['pending','approved','rejected'],default:'approved'},createdAt:{type:Date,default:Date.now}});module.exports=mongoose.model('Content',s);
